{
  "name": "xeno-crm-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run install:backend && npm run install:channel",
    "install:backend": "cd backend && npm install --omit=dev && npx prisma generate",
    "install:channel": "cd channel-service && npm install --omit=dev",
    "start": "node start-all.js"
  }
}
